user_input = input("Enter a string: ") # prompt for string
# set flags
# for each character, if letter then mark found_letter
    # if not lowercase, set all_lower to false and break
# print result (true if all letters are lowercase and at least one letter exists, else false)
